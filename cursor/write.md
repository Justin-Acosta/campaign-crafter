Use the todo_write tool to complete the following request from the dungeon master:

1. Read the contents of all files in the  `players/`, `chapters/`, `characters/`, `documents/`, `lore/`, `dungeons/`, `random-encounter-tables/` and `side-quests/` directories to gather context about the campaign.

2. Read `preferences.md`. The rules in `preferences.md` MUST be followed when generating file changes.

3. Make file changes according to the dungeon master's request.

4. Read `preferences.md` again and read each of your file changes. For each file change, determine if the file change follows all of the rules in `preferences.md`. If a file change can be improved to better align with the rules in `preferences.md`, update the file change with your improvements.
